/// <reference types="es6-promise" />
export declare class BPClient {
    hostname: string;
    port: number;
    constructor(bpUrlValue: string);
    setSynchronization(enabled: boolean): Promise<{}>;
    isSyncEnabled(): Promise<{}>;
}
